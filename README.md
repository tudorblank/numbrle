Simple Wordle-like game based on numbers.

Contains 2 game modes:
- Unique digits
- Non-unique digits

## Rules
- Each guess consists of 5 digits (0–9)
- A maximum of 6 attempts is allowed
- Green: correct digit in the correct position
- Yellow: correct digit in the wrong position
- Red: digit not present

Requires a C compiler (such as `gcc` or `clang`) to build the executable.
